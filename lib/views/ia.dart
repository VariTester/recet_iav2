import 'package:flutter/material.dart';
class Ia extends StatelessWidget {
  const Ia({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PARTE DE IAaaaaaaaaaaaaaaaaa'),
    );
  }
}